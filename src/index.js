import React from "react";
import ReactDOM from "react-dom/client";
import SalaryApp from "./SalaryApp";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<SalaryApp />);
